# maxd6 build: auto-ladder 4..6 Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use conclave:executing-plans to implement this plan task-by-task.

**Goal:** Extend the confidence-gated spec round from ceiling 5 to 6 (7-lane
verify, depth-6 draft) shipped ONLY as a depthctl auto-ladder 4..6, per the
2026-07-08 GO verdict in docs/maxd6-decision.md (its items 1-7 checklist is the
spec; line anchors there are 07-07-era, re-locate before editing).

**Architecture:** One more explicit lane in the P12b style (pointer-array
refactor rejected: d6 only, d7/d8 deferred). Ladder in depthctl.h (CPU-tested)
generalizes P13 promote/demote to levels 4..6 with per-level sat/yield EMAs.
Binary defaults unchanged: gate stays opt-in; auto ceiling becomes 4..6.

**Success gates (from the verdict):** width-7 lane cost MEASURED (forced-cap
sweep) before any recommendation; canonical 4c4120c7 EXACT (ungated + gated
4/5/6/auto); test_kernels + test_depthctl ALL PASS; gated round determinism;
cctx replay d6 >= +3% vs d5; no envelope payload below its d5 baseline;
glf/gla observable at lane 6.

**Tasks:**
0. Branch maxd6-ladder; baseline gates.
1. depthctl ladder 4..6 (TDD: promote 4->5->6, demote 6->5->4, per-level
   seeds/hysteresis/exclusivity, bars shared via hi/lo).
2. Engine widening (the bulk, maxd6-decision.md items 1-5): gate_maxd clamp 6;
   perm mod-7 + graph arrays [6]->[7] (spec/draft/verify/draft_lo/spec_sample,
   verify_graph_w -> [8][7], draft_step_graph -> [6][7]); S_spare6/ring_spare6
   (+157 MB) + ternary chains + alloc/memset/snapshot; 7th verify lane through
   the ~19 buffer bundles (_g lane), mm5->out_g, qx5->qx6, gemv case 7,
   logits2 7*VOCAB, fd scratch 7 lanes, d_draft6/h_next6/d_pos_m6/margin[6],
   outcome buffer n<=7 ([0]=n,[1..7]=tokens,[8]=pending) + emit[] callers,
   quantize3 explicit-pointer lane fix (THE landmine) + audit all per-lane
   selects; sampled path untouched (capped 4); warm width-7 graphs.
3. Telemetry: gch[7]/gnh[8]/glf[7]/gla[7] + server prints.
4. Gates: build sm_86+sm_120 clean; test_kernels; canonical x5 configs;
   determinism (2 runs same rounds); deep gated identity (cctx d5-vs-d6 text
   byte-identical). On divergence: the P12b bisect recipe (theta=100 width-2
   PASS -> narrow; low theta cap<=5 PASS -> width-7 only; Q27_FD=v1 rules
   attention out; explicit-pointer lane bugs are memcheck-blind).
5. Width-7 lane cost: forced-deep sweep on cctx (Q27_PMIN=0.05) d5 vs d6
   ms/round -> per-lane increment; replaces the extrapolation in the verdict.
6. Replay A/B: cctx d4/d5/d6/auto + envelope (echo docs codegen testgen
   docs61k) auto vs d5. Verdict gates above.
7. BUILDLOG + decision-doc close-out; merge master; push; memory update.
